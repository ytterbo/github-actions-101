name: "Hello Actions!"

on:
  workflow_dispatch:
  push:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Echo Greeting"
        run: "echo 'Hei på deg GitHub Actions!'"
      - name: "Echo Weather"
        run: "echo 'Fint vær i dag!'"
      - name: "Echo Goodnight"
        run: "echo 'Natta!'"